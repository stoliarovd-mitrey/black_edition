const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;
if(!reduced&&'IntersectionObserver' in window){const ob=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');ob.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>ob.observe(e));const counter=document.querySelector('[data-count]');const co=new IntersectionObserver(es=>{if(es[0].isIntersecting){co.disconnect();const start=performance.now();function tick(now){const p=Math.min(1,(now-start)/1000);counter.textContent=Math.round(19*(1-(1-p)**3));if(p<1)requestAnimationFrame(tick)}requestAnimationFrame(tick)}},{threshold:.6});co.observe(counter)}
const dialog=document.querySelector('#model-dialog');document.querySelectorAll('[data-model]').forEach(button=>button.addEventListener('click',()=>{const section=document.getElementById(button.dataset.model);const src=section.querySelector('img');document.querySelector('#dialog-image').src=src.getAttribute('src');document.querySelector('#dialog-image').alt=src.alt;document.querySelector('#dialog-title').textContent=section.querySelector('h2').textContent;document.querySelector('#dialog-device').textContent=section.querySelector('.item__label').textContent;document.querySelector('#dialog-details').replaceChildren(...[...section.querySelectorAll('.item__body')].map(p=>p.cloneNode(true)));dialog.showModal()}));document.querySelector('.dialog-close').addEventListener('click',()=>dialog.close());dialog.addEventListener('click',e=>{if(e.target===dialog){const r=dialog.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)dialog.close()}});
