# Black Edition

Landing page for the Caviar Black Edition collection.

## Structure

- `index.html` — page markup
- `style.css` — responsive styles
- `page.js` — interactions and hero video
- `assets/` — image, video and font assets

The page expects the production assets from the supplied Black Edition media archive to be placed in `assets/`.
